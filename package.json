{
  "name": "archflow",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "preview": "turbo run preview"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@10.2.0"
}
